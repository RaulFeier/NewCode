#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;

  bool found = false;

  while (true) {
    if (n == 10) {
      found = true;
    }
  }

  if (found) {
    cout << "LAM GASIt";
  } else {
    cout << "NU LAM GASIT";
  }

  return 0;
}