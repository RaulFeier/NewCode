#include <bits/stdc++.h>

using namespace std;
using i32 = long;
using i16 = short;

int main() {
  i32 n;
  cin >> n;

  vector<vector<i32>> costs(n);

  short m = 0;
  i32 index = 1;
  i32 total = 1;
  for (i32 i = 0; i < n; i++) {
  }

  return 0;
}