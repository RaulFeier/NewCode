#include <bits/stdc++.h>

using namespace std;

int N;
vector<vector<int>> dp(N, vector<int>(N, -1e9));
vector<vector<int>> arr(N, vector<int>(N));

int sol(int dir, int sum, pair<int, int> index) {}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);

  cin >> N;

  for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
      cin >> arr[i][j];
    }
  }

  return 0;
}