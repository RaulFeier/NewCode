#include <bits/stdc++.h>

using namespace std;

int add(int a, int b) { return a + b; }

int main() {
  int n, m;
  cin >> n >> m;

  vector<int> v(n);

  for (int i = 0; i < n; i++) {
    cout << "eep" << endl;
  }

  for (int i = 0; i < n; i++) {
    cout << add(i, n) << endl;
  }

  return 0;
}