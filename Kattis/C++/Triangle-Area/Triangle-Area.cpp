#include <bits/stdc++.h>

using namespace std;
using i64 = long long;

int main()
{
    double h, b, a;
    cin >> h >> b;
    cout.precision(1);

    a = (h * b) / 2;
    cout << fixed << a << endl;
}