#include <iostream>
using namespace ::std;

int main()
{
    int a, b, c, d, e, f;

    cin >> a >> b >> c >> d >> e >> f;

    cout << -1 * (a - 1) << " " << -1 * (b - 1) << " " << -1 * (c - 2) << " "
         << -1 * (d - 2) << " " << -1 * (e - 2) << " " << -1 * (f - 8) << endl;

    return 0;
}