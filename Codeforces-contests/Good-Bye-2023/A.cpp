#include <bits/stdc++.h>

using namespace std;
using i64 = long long;

int main() {
  i64 tc;
  cin >> tc;

  while (tc--) {
    i64 n, k;
    cin >> n >> k;

    vector<i64> v(n);

    for (i64 i = 0; i < n; i++) {
      cin >> v[i];
    }
  }

  return 0;
}